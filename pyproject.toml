[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-social-mvp"
version = "0.1.0"
description = "Minimal local MVP for an agent/human social app sidecar."
requires-python = ">=3.10"

[project.scripts]
agent-social = "agent_social.cli:main"
